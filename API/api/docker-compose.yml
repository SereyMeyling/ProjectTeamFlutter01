services:
  web-content-api:
    container_name: web-content-management-api
    image: web-content-management-api:latest
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "30033:30033"
    environment:
      - TZ=Asia/Phnom_Penh
    restart: unless-stopped
    volumes:
      - /opt/projects/teach/upload/images:/opt/projects/teach/upload/images